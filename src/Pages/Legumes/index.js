import React from 'react';
import { View } from 'react-native';
import List from '../../Components/Legumes';

export default function Legumes() {
 return (
   <View style={{flex: 1, backgroundColor: "#FFF"}}>
      <List/>
   </View>
  );
}