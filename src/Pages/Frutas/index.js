import React from 'react';
import { View } from 'react-native';
import List from '../../Components/Frutas';

export default function Frutas() {
 return (
   <View style={{flex: 1, backgroundColor: "#FFF"}}>
      <List/>
   </View>
  );
}